import { ClockLoader } from "react-spinners";

const Spinner = ()=>{
    return (
      <center>
        <ClockLoader className="clk" color="#00ff35" />
      </center>
    );  
}

export default Spinner;